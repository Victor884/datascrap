# Dados processados

Base canônica gerada a partir de `data/raw` pelo pipeline de limpeza.

Arquivo esperado:

- `vagas-processadas.csv`

Use esta camada para consumo pela API, análises e futuramente pelo banco relacional.
